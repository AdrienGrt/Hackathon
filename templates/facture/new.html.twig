{% extends 'base.html.twig' %}

{% block title %}Créer une nouvelle Facture{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="card shadow-lg p-4">
        <h1 class="text-center mb-4"><b>Créer une nouvelle Facture</b></h1>

        
        <div class="mt-4">
            {{ include('facture/_form.html.twig', { 'button_label': 'Créer' }) }}
        </div>

        
        <div class="text-center mt-4">
            <a href="{{ path('app_facture_index') }}" class="btn btn-secondary btn-lg rounded-pill px-5">
                Retour à la liste
            </a>
        </div>
    </div>
</div>
{% endblock %}
